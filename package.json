{
  "name": "jparty-app",
  "displayName": "JParty",
  "author": "CODE JParty Project",
  "description": "Party lighting!",
  "version": "1.0.0",
  "repository": "https://github.com/code-jparty/jparty-app",
  "scripts": {
    "start": "npm run prepare && cordova run browser",
    "android": "npm run prepare && cordova run android",
    "ios": "npm run prepare && cordova run ios",
    "prepare": "npm install && tsc",
    "clean": "rimraf www/js && rimraf node_modules"
  },
  "dependencies": {
    "cordova-android": "^7.1.1",
    "cordova-browser": "^5.0.4",
    "cordova-plugin-whitelist": "^1.3.3",
    "react": "^16.5.2"
  },
  "devDependencies": {
    "@types/react": "^16.0.36",
    "typescript": "^3.0.3",
    "rimraf": "^2.6.2",
    "ts-node": "^7.0.1"
  },
  "cordova": {
    "plugins": {
      "cordova-plugin-whitelist": {}
    },
    "platforms": [
      "browser",
      "android",
      "ios"
    ]
  }
}